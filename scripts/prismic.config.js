window.prismicConfig = {
  /**
   * Update the properties below with your Prismic repository information to enable live content.
   * repositoryName: The name of your repository (e.g. curious-site -> use "curious-site").
   * documentType: The API ID of the custom type that stores this page (defaults to "homepage").
   * documentUID: The UID of the document (defaults to "homepage").
   * accessToken: Optional – required only when your repository is private.
   */
  repositoryName: "curious-garden",
  documentType: "homepage",
  // leave blank to fetch the first published document of this type
  documentUID: "",
  accessToken: "",
};
